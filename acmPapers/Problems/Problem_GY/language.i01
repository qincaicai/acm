PROGRAM SAMPLE;
CONST
  TRUE = 1;
  FALSE = 0
VAR
  INTEGER :A;
  INTEGER :B;
  INTEGER :ARRAY C[2]
BEGIN
  C[0] := 2;
  C[1] := 3;
  A := C[0] + C[1];
  B := C[0] * C[1];
  PRINT A;
  PRINT B;
  IF A = B THEN PRINT TRUE ELSE PRINT FALSE
END.

